# Claim 5 source audit

Corollary 3 follows H8 and Lemma 1 in the primary source. It assumes H5, H2,
and H8, chooses `pi=mu tensor nu*`, and states that Theorem 4 holds with the
joint quantities supplied by Lemma 1.

H8 requires each marginal to have a positive C2 density, L8 score, L2 score
Hessian, and weak-log-concavity parameters. Lemma 1 then supplies bounds for
the joint score and Hessian and sets

`alpha_pi=min(alpha_mu,alpha_nu*)`,
`M_pi=2 max(M_mu,M_nu*)`.

This contract therefore constructs the product explicitly, audits both
marginals separately, and checks the block-joint relations. Reusing a KL run
or a comment that “the flow still works” is not evidence for the corollary.
