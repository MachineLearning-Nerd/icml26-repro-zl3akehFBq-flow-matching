# Claim 3 evaluation

Verdict: **VERIFIED**

The exact implicit schedule identity holds to floating-point precision. In 18
formula-independent exact-KL first-hit searches, the nonuniform schedule uses
1.87 to 13.56 times less work. Every immediately smaller resource misses its
target. The advantage grows toward the endpoint, and the symbolic schedule
certificate gives logarithmic endpoint work and cubic dimension order.

The previous 3D comparison (`0.0452` non-uniform versus `0.0042` uniform) is
retained as a negative control: “both below 0.5” is rejected as evidence of
faster convergence.
